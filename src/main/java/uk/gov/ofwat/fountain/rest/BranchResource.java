package uk.gov.ofwat.fountain.rest;

import javax.ws.rs.Path;

@Path("/branches")
public class BranchResource extends RestResource {
	/*
	 * TODO - Be able to get a list of all branches - 
	 * may want to restrict these to the type that are Runs only?
	 * 
	 * How are we going to deal with tags? Data items are tagged but
	 * how are these data items and tags related to branches/Runs? 
	 * 
	 */
}
